ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.utn.tacs.ApplicationKt.module ]
    }
}

db {
    url = localhost
    port = 27017
    name = tacs
}

cache {
    countries = 3600 // seconds
}

adminUser {
    name = "the Big Boss"
    email = "admin-root@gmail.com"
    pass = "adminutn"
}

jwt {
    secret = "secret-super-key"
    issuer = "tacs"
    validity = 36000000
}